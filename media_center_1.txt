# Media Center 交接摘要

## 当前状态

- Go 项目：`C:/Users/Cirno/dev/elbot`
- Media Center、Hook、Skill、Elnis 媒体生命周期已完成；文档和测试已同步。
- 当前停在平台适配之前，不开始平台消息引用回复消费。
- active plan 已恢复：1、2、3、4、6 已完成；5 平台适配、7 引用回复消费未完成。
- 详细设计、字段约定、边界和测试说明：阅读仓库根目录 `media_center_2.md`。

## 本轮关键决定

- Elwisp image/file 复用 `url`：`url: "media:<sha256>"`，不新增独立媒体字段。
- Hook segment 继续复用 `url/path/base64` 承载完整媒体 ID。
- 显式媒体 API 统一使用字段 `media`，包括 `send_file`、Skill `media_inputs`、Hook `media.*` API。
- 尚未发布的 Session、Tool Transcript、报告和 Cron JSON 使用 `media`；内部 Go `MediaID`、SQLite `media_id` 列保留。
- 旧 Session、旧 fork、旧 Cron 不迁移、不批量下载、不回填媒体引用。
- 媒体引用归零后保留持久化 1 小时孤儿宽限期；关闭进程不丢计时，下次维护任务继续处理。

## 已验证

- `gofmt` 已执行。
- 相关包测试通过。
- `go test -p 1 ./...` 全部通过。
- fork 检查点范围、Cron/消息引用审计、消息替换回滚、取消后的 Elnis 终态清理均有回归测试。
- `git diff --check` 通过；`media_center_1.txt` 有 Windows LF/CRLF 提示，不是代码空白错误。

## 接下来

1. 新窗口先阅读 `AGENTS.md`、`media_center_2.md`、本文件和当前 diff。
2. 执行 active plan 第 5 项：QQ OneBot、QQ Official、Telegram 平台媒体适配，保持平台只提供原始来源并延迟物化。
3. 检查平台发送 `media`、Chat History 原始媒体引用和真实唤起时机，补相关测试及中文文档；不要修改英文镜像或 CHANGELOG。
4. 第 5 项完成后暂停，不开始第 7 项。
